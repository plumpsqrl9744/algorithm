function solution(n) {
    

    var answer = "";
    for(let i = 0; i <= n; i++){
        
        if (i==1){
            answer = "수";
        }else{
            answer += i%2==0 ? "박" : "수";
        }
    }
    return answer
}